Session wrap-up. 1) List durable findings from this session (decisions + why, gotchas, load-bearing files discovered). 2) Fold them into the project's root CLAUDE.md/AGENTS.md orientation sections — keep it under its 150-line cap, deleting stale lines to make room. 3) Append decisions to the project's `docs/decisions.md` (create if absent) as dated rows: `| YYYY-MM-DD | decision | why | files |` — this file is the durable decision record, so write the rationale, not just the outcome. 4) If docs changed, rerun ~/harness/agent/core/gen-doc-index.sh. Note: verbatim session history is separately searchable via `mempalace search "<query>"` (auto-mined by hooks) — do not duplicate raw history here, only distilled knowledge. Report what was written where. $ARGUMENTS
